# تقرير الجاهزية النهائية لـ FirstProAccounting

**التاريخ:** 19 أغسطس 2026
**الفرع:** `main`
**المستودع:** [Nagmix/FirstProAccounting](https://github.com/Nagmix/FirstProAccounting)
**إعداد:** Manus AI

## الملخص التنفيذي

اكتملت وحدات التطوير المعتمدة في خطة FirstProAccounting على الفرع `main`. شمل التنفيذ النواة المالية، الخدمات والصيانة، الإنتاج والوصفات، تحويل الوحدات وتسويات المخزون، مراجعة الضرائب والعملات والفترات والإلغاءات، التقارير ودفتر الأستاذ والتدقيق، ثم واجهات Android والنسخ الاحتياطي وحراس القبول.

> **نتيجة التحقق:** تشغيل GitHub Actions النهائي رقم [32196197220][1] انتهى بنجاح، ونجح فيه `flutter analyze`، ونجحت **877 اختباراً**، ثم بُني APK وAAB للإصدار، وتم التحقق الصريح من توقيعهما واستخراج بصمات keystore ورفع artifact النسختين.

لا يتوفر Flutter محلياً في بيئة العمل، ولذلك جرى الاعتماد على بوابة Flutter وAndroid في GitHub Actions باعتبارها بيئة التحقق التنفيذية. لا توجد ملفات معلقة في الفرع بعد دفع تحديث التوثيق النهائي والتحقق من CI على الالتزام الأخير.

## مصفوفة الإنجاز والتحقق

| المجال | ما نُفذ | الدليل الرئيسي | الحالة |
|---|---|---|---|
| النواة المالية | `MoneyValue` و`MoneyEngine` و`JournalValidator` و`TaxEngine` و`InvoiceTotalsEngine` و`CurrencyEngine` | اختبارات النواة والبوابات المتتابعة | مكتمل |
| الخدمات والصيانة v57 | أوامر الخدمة، الأجهزة، الضمانات، الدفعات، الترحيل والإلغاء بقيود عكسية | التشغيلان [32097217640][2] و[32097847543][3] | مكتمل |
| الإنتاج والوصفات v58 | schema وmigration، نماذج الإنتاج، `ProductionService`، استهلاك الخام، المنتج التام، التكلفة، rollback، منع الترحيل المكرر والإلغاء العكسي | التشغيل [32189264681][4] | مكتمل |
| تحويل الوحدات والجرد | `UnitConversionPolicy` و`InventoryAdjustmentService`، تحويل إلى الوحدة الأساسية، قيد تفاوت متوازن، حاجز السالب، منع التأكيد المكرر | التشغيل [32190242201][5] | مكتمل |
| الضرائب والعملات والفترات | رفض معدل الصرف الصفري والسالب، تقريب الضريبة، حماية الفترة المغلقة، الإلغاء غير الحاذف للأصل | التشغيل [32191659066][6] | مكتمل |
| التقارير ودفتر الأستاذ | استخدام `amount_base` التاريخي، منع إعادة التقييم بسعر صرف حالي، إظهار العملة ومعدل الصرف والقيمة الأساسية في دفتر الأستاذ | التشغيل [32192332762][7] | مكتمل |
| واجهات Android | شاشات الإنتاج والجرد، المسارات، القائمة الجانبية، واختبارات Widget والقبول | التشغيل النهائي [32196197220][1] | مكتمل |
| النسخ الاحتياطي وجاهزية Android | حارس حماية النسخ بكلمة مرور، تعطيل النسخ العام لقاعدة SQLCipher، وتفعيل callback الرجوع الحديث | التشغيل النهائي [32196197220][1] | مكتمل تقنياً |

## الضوابط المحاسبية التي تم الحفاظ عليها

تستخدم حركة المخزون وتكلفة الإنتاج والمواد الخام العملة الأساسية **YER** مع `exchange_rate = 1.0`. تُحسب القيود داخل معاملات SQLite الذرية، مع إبقاء عمليات الحصول على offset للعملة الأساسية خارج المعاملة لتجنب deadlock. لا تُحذف القيود الأصلية عند الإلغاء؛ بل تُنشأ قيود عكسية جديدة تحمل مرجعاً واضحاً إلى العملية الأصلية.

تعتمد التقارير متعددة العملات على `amount_base` الموقّت عند الترحيل، وليس على إعادة ضرب المبالغ التاريخية بسعر صرف حالي. ويُحظر تعديل `current_stock` مباشرة؛ إذ تمر تحديثات الرصيد من خلال تحديثات قاعدة البيانات داخل المعاملة مع تسجيل حركة مخزون قابلة للتدقيق.

## التحقق الاختباري والبنائي

تضمن تشغيل CI النهائي رقم [32196197220][1] العناصر التالية بصورة ناجحة: التحليل الساكن، تشغيل كامل اختبارات Dart وFlutter، بناء Release APK بحجم 114M، بناء Release AAB بحجم 91M، التحقق من توقيع APK عبر `apksigner`، التحقق من توقيع AAB عبر `jarsigner` وعبارة `jar verified`، استخراج بصمات SHA-1 وSHA-256، ورفع ملفي الإصدار. سجل التشغيل يثبت عبارة `877 tests passed`، ويثبت أن APK استخدم APK Signature Scheme v2، كما يثبت نجاح رفع artifact باسم `release-apk` برقم `9346052586` و`release-aab` برقم `9346053771`.

تشمل الاختبارات الجديدة اختبارات سياسة الوصفات، ترحيل الإنتاج والتكلفة والهالك، تحويل الوحدات، تسويات الجرد والـ rollback والحاجز السالب، الضرائب والعملات والفترة المغلقة، التقارير التاريخية متعددة العملات، مسارات وشاشات الإنتاج والجرد، وجاهزية Android والنسخ الاحتياطي.

## مراجعة الجودة النهائية

من ناحية **الصحة**، أُضيفت اختبارات للحالات السعيدة وحالات الرفض والتكرار والفشل الذري والإلغاء. ومن ناحية **الهندسة**، أُبقيت الخدمات ضمن طبقة مصادر البيانات، والسياسات المالية ضمن `core`، وسُجلت الخدمات الجديدة عبر GetIt دون تغيير الترحيلات القديمة من v2 إلى v56. ومن ناحية **الأمان**، لم تُضف أسرار أو اعتماديات جديدة، واستمرت استعلامات SQLite باستخدام معاملات وقيم مربوطة. ومن ناحية **الأداء**، بقيت عمليات المخزون والقيود في معاملات واحدة، وتجنبت الخدمة استدعاء خدمة العملة داخل المعاملة.

## ملاحظات تشغيلية غير حاجزة

أظهر GitHub Actions تحذيرات صيانة تخص إجبار بعض إجراءات GitHub على Node.js 24 رغم استهدافها Node.js 20، وتحذيراً بأن `setup-java@v4` قديم ويوصى مستقبلاً بالترقية إلى `setup-java@v5`. هذه التحذيرات لم تفشل التحليل أو الاختبارات أو بناء Android، لكنها تستحق تحديثاً مستقلاً في دورة صيانة لاحقة لا يخلط بين تحديث CI وتغيير المحاسبة.

لم يُنفذ اختبار يدوي على جهاز Android فعلي أو محاكي داخل هذه البيئة؛ إثبات الجاهزية الحالية هو إثبات بناء واختبارات وWidget ومسارات عبر CI. قبل النشر التجاري، ينبغي تنفيذ قبول تشغيلي على جهاز Android حقيقي بقاعدة بيانات اختبار، ثم اختبار استعادة نسخة احتياطية قبل وبعد ترقية التطبيق، ومراجعة دليل الحسابات والضرائب من محاسب محلي وفق متطلبات اليمن أو البلد المستهدف.

## حالة المستودع

آخر commit برمجي موثق هو `1d42437` بعنوان `test: use flutter test harness for signing regression`، وهو متزامن مع `origin/main` وقت التحقق. أصلح هذا الالتزام فشل `flutter analyze` الناتج عن استيراد `package:test/test.dart` غير المتاح مباشرة، باستخدام `package:flutter_test/flutter_test.dart` المتوافق مع اعتماديات المشروع. نجاح التشغيل [32196197220][1] يثبت بقاء الفرع قابلاً للتحليل والاختبار والبناء والتوقيع بعد الإصلاح.

## المراجع

[1]: https://github.com/Nagmix/FirstProAccounting/actions/runs/32196197220 "CI النهائي: 877 اختباراً وبناء Android والتحقق الصريح من التوقيع"
[2]: https://github.com/Nagmix/FirstProAccounting/actions/runs/32097217640 "CI ترحيل وإلغاء أوامر الخدمة"
[3]: https://github.com/Nagmix/FirstProAccounting/actions/runs/32097847543 "CI قبول الخدمات والتوثيق"
[4]: https://github.com/Nagmix/FirstProAccounting/actions/runs/32189264681 "CI خدمة ترحيل الإنتاج"
[5]: https://github.com/Nagmix/FirstProAccounting/actions/runs/32190242201 "CI تحويل الوحدات وتسويات الجرد"
[6]: https://github.com/Nagmix/FirstProAccounting/actions/runs/32191659066 "CI الضرائب والعملات والفترات"
[7]: https://github.com/Nagmix/FirstProAccounting/actions/runs/32192332762 "CI التقارير ودفتر الأستاذ"
